# MEMORY

Long-term memory. This file is injected verbatim into every conversation, so keep it compact and
true. You can edit it by hand, but it is mostly **curated automatically**: notes you write to
`memory/<date>.md` that you keep coming back to get promoted here by nightly "dreaming", each with a
provenance tag like `[score=… recalls=… source=…]` and an HTML comment marker.

<!-- Promoted memories will be appended below under dated "## Promoted From Short-Term Memory" sections. -->
